pipeline {
    agent any
	stages {
	stage('Initialize'){
            steps {
                sh 'chmod -R 777 ${WORKSPACE}'
            }
        }
	stage('Build Customer image'){
		steps{
    		sh "cd /var/lib/jenkins/workspace/Create_Customer_MSR_Image"
		sh "docker build -t msr:customer$BUILD_NUMBER ."
		echo "Custom image created"
	    }
	}
	}
}
